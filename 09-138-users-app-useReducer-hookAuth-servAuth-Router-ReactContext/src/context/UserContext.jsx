import { createContext } from "react";

export const UserContext = createContext();                                                 //con esto se crea un nuevo contexto para todos los usuarios. aquí se va a almacenar la información que se va a compartir en los componentes que se relacionan a los usuarios.